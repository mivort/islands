/** Available data fields. */
export enum Data {
	/** Text label drawn over the node. */
	NAME = 'name',
	/** Selected node shape. */
	SHAPE = 'shape',
	/** Node visible size. */
	SIZE = 'size',

	REF = 'ref',
	DESC = 'desc',
	DOC = 'doc',
}
